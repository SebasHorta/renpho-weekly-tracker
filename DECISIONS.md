# Design decisions

A record of the non-obvious calls made while building this, and why — mostly for my own future reference. Grouped by the phase each decision was actually made in; see the main [README](README.md) for the phase plan and current status.

## Phase 0 — spike

- **The `get_body_composition_measurements()` method doesn't exist.** Early planning assumed this method (referenced in some docs/examples) would sidestep a known truncation bug on BIA/smart scales. Reading the actual installed `v0.1.0` source (`renpho/client.py`) showed it was never shipped — only `get_all_measurements()` and the lower-level `get_measurements(table_name, user_id, total_count)` exist.
- **The truncation bug is real, but the fix is our own, not the library's.** `get_device_info()` reports a `count` per scale table, and `get_measurements()` stops paginating once it collects that many records. If Renpho's server under-reports `count`, real history gets silently cut off. Our fix: request `count + COUNT_PADDING` (200) instead of the exact reported count. This is safe because `get_measurements()` independently stops on the first empty page — overshooting the count is harmless, undershooting it isn't. See `fetcher.py`.
- **No session/token persistence.** `renpho-api` re-authenticates via `login()` on every run, with no exposed way to cache a session. Confirmed acceptable for a manual, once-a-week script.

## Phase 1 — ingest + store

- **Idempotency key is Renpho's own `id` field, not `timeStamp`.** Verified by pulling one raw record and inspecting its keys (`scripts/exploration/inspect_fields.py`) — `id` is a distinct, stable field separate from `timeStamp` and the account's `bUserId`. Using it as the SQLite primary key with `INSERT OR REPLACE` means re-running the ingest never creates duplicate rows.
- **Zeroed bioimpedance fields mean "not measured," not "measured as zero."** Early records show `bodyfat`, `water`, `muscle`, etc. all at `0.0` while `weight`/`bmi` are real — this happens when a weigh-in doesn't register full foot contact with the scale's sensors. `store.py` stores the raw value exactly as returned (no cleaning at the storage layer); `analysis.py` is responsible for treating `0` as missing before averaging these fields.
- **`invalidFlag` looked like a built-in data-quality signal — it isn't.** Hypothesis tested empirically (`scripts/exploration/inspect_invalid_flag.py`) across all 1,553 records: `invalidFlag=3` (1,125 records) and `invalidFlag=0` (428 records) have statistically identical rates of zeroed bodyfat readings (0% vs 1%). Whatever the flag actually encodes, it isn't reading validity. Dropped from the schema; the outlier check below is our only data-quality safeguard.
- **Outlier safeguard: flag, don't discard.** `store.py`'s `OUTLIER_THRESHOLD_LB` (currently 10) flags any reading that differs from the prior one by more than that many pounds. Flagged rows stay in the database and appear in the report with a warning, but are excluded from weekly averages by default — since a big swing might be real (illness, dehydration) rather than a bad reading, and shouldn't be silently deleted either way.

## Phase 2 — weekly averages

- **One reading per calendar day (keep the latest).** A day with two weigh-ins (e.g. a re-check after a workout) would otherwise double-count toward that week's average. `analysis.py`'s `dedupe_to_daily()` collapses each day to its latest reading, capping weekly counts at 7. Done in `analysis.py`, not `store.py`, so SQLite keeps every raw reading.
- **Bucket by `timeStamp` (UTC epoch → America/New_York), NOT the stored `localCreatedAt`.** Initially indexed on `localCreatedAt` on the assumption it was the device's true local time (which would handle travel correctly). That was wrong: `localCreatedAt` is corrupted, running ~8 hours ahead of true UTC, which pushed afternoon weigh-ins into the next calendar day and misassigned them to the wrong week. Verified against the RENPHO app (which buckets by `timeStamp`) — converting the `timeStamp` epoch to the fixed home timezone (`America/New_York`, confirmed no cross-tz travel) reproduces the app's daily values and realistic morning/afternoon weigh-in times exactly. See `load_measurements()` in `analysis.py`.
- **Weeks run Sunday-Saturday**, via `pandas.resample('W-SAT')` rather than the pandas default (`'W'`, which ends weeks on Sunday). Matches how weigh-ins are actually tracked day-to-day, and `.mean()` over however many days were logged that week (5, 6, or 7) requires no extra handling.
- **Weight is stored/averaged in full precision, rounded only at display.** The scale's true value is kg (0.05 kg grid); the RENPHO app displays lb snapped to the nearest 0.2 lb (its lb-mode display resolution — which is why single readings like 174.94→175.0 and 175.49→175.4 look "inconsistently" rounded but aren't). We keep full-precision kg internally and round only in the display layer, since averaging pre-rounded values needlessly loses accuracy.

## Phase 3 — interactive HTML report

- **The chart is inline SVG, not matplotlib.** The original plan allowed either. Loaded the project's dataviz design skill before building it and went with hand-built SVG: keeps `report.html` fully self-contained (no ~30MB matplotlib dependency), and SVG themes with light/dark mode, which a baked PNG can't.
- **The report renders client-side in JavaScript, not Python.** Started as pure Python string-rendering. Re-architected when an adjustable week-count control needed to survive a plain page reload — which never runs Python. `report.py` now embeds the *entire* weekly (and daily) history as JSON in the page; a JS layer (a direct, comment-linked port of the original Python rendering functions, which were deleted rather than kept as an unsynced duplicate) renders the chart and table from it, driven by `localStorage`-backed controls (window size, ◀▶ history paging, table sort order, goal-trend start date). Paging position always resets to "now" on load; the other preferences persist.
- **Date/time formatting happens in Python, never in JS.** Every date string, week range, and the year(s)-spanned label the chart needs are precomputed server-side and embedded as plain strings/numbers. Given the `localCreatedAt` timezone bug from Phase 2, re-deriving dates client-side felt like reopening the same risk for no benefit — the browser only ever handles numbers and pre-formatted text.
- **Table dates match the chart's "Week of" framing.** Initially the table showed just the week-ending date; switched to a full span (`Jul 19 – Jul 25, 2026`) alongside the chart's range labels, since "Week of `<end-date>`" would misleadingly imply the week starts there.
- **Weekly-average precision vs. the scale's display precision, applied to the report's display layer.** A weekly *average* is a computed trend signal that can meaningfully move by less than the scale's 0.2 lb display grid. Daily values (`analysis.py`'s `snap_to_scale_lb()`) round to match the scale; weekly averages/deltas in the report keep 1 decimal of real precision, so a real sub-0.2 lb weekly trend doesn't get rounded away to a false "no change."
- **Goal weight is tied to `GOAL_MODE`, not hardcoded to "down."** The in-progress week's target (1% off the prior completed week's average) flips direction automatically with the existing cut/bulk/neutral setting, rather than assuming a cut — stays correct without a second edit if the goal ever changes.
- **The target trend anchors to a fixed start point, not a rolling window.** Considered projecting 1%/week from whatever week happens to be leftmost in the current view (simpler, no state) but rejected it: a "target" that changes depending on what you're scrolled to isn't really a target. Instead it's a single fixed baseline (whichever week's actual average you set as your "Start date") that 1%/week compounds forward from regardless of the viewing window — closer to how a real diet-progress line should behave. The date picker writes to `localStorage`, same pattern as "weeks shown," so it survives reload/regeneration without touching `report.py`.
- **The 1-week chart view plots daily readings, not one dot.** A single selected week has only one weekly average — not a trend. When "Weeks shown" is `1`, the chart switches to that week's individual daily readings (a full daily dataset embed + a parallel `renderDailyChart()`) instead of showing a lone point. The table stays a one-row weekly summary regardless — Goal/Target/deltas are inherently weekly concepts with nothing daily to switch them to. Daily values use `snap_to_scale_lb()` for the same reason as elsewhere: individual readings should match what the scale actually showed.

## Phase 5 — automated weekly email

- **Local `launchd`, not a cloud cron (e.g. GitHub Actions).** A cloud job would run regardless of whether the Mac is on, and would make a hosted link nearly free. Rejected anyway: Renpho credentials would have to live as a cloud secret, the SQLite database (the project's whole source-of-truth architecture) becomes awkward to persist between ephemeral runs, and a reverse-engineered API is more likely to get rate-limited or blocked when called from a datacenter IP than from the home IP it's always been tested against. Local keeps credentials on-machine and matches the project's existing privacy posture.
- **Numbers inline in the email, no hosted report.** A link to the full interactive report would require putting `report.html` (your actual weight/body-fat history) at a public URL — even an "obscure" one is effectively public to anyone with the link. Decided the weekly email should just contain the numbers directly (This week / Last week / Change), so the privacy tradeoff of public hosting never has to be made at all. Revisit only if wanting the interactive chart specifically becomes worth that tradeoff.
- **Daily schedule with a stateful once-per-week guard, not a single precisely-timed weekly cron.** A cron firing at exactly "Sunday 9 AM" is fragile — if the Mac is asleep at that exact moment, `launchd` catches it up on wake, but a *weekly* schedule risks the catch-up landing on the wrong day or being missed if sleep persists past the window. Running `weekly_check.py` daily and tracking "have I already emailed this completed week" in a new `notifications` SQLite table (see `store.py`'s `has_notified`/`mark_notified`) sidesteps timing fragility entirely: whichever day the job next happens to run, it sends the newly-completed week exactly once.
- **A failed send does not mark the week as notified.** `weekly_check.py` calls `send_email()` before `mark_notified()`, and `send_email()` raises on any failure (bad credentials, network, Gmail hiccup) rather than swallowing it. If sending fails, the week stays unmarked, so tomorrow's run retries it instead of silently losing that week's summary.
- **The same broad `except Exception` as `run.py`, reused deliberately.** `weekly_check.py`'s fetch step wraps the same wide net for the same reason: an unofficial API that "can break without warning" shouldn't stop a headless job from at least checking whether it owes an email for already-synced data.
- **Email body uses HTML entities, not raw unicode glyphs.** First version used real `–`/`·`/▲/▼ characters directly in the HTML string; opening the raw body as a local file for preview showed mojibake (a charset-guessing artifact of viewing it outside a proper MIME-declared email). Switched every special character to its HTML entity (`&ndash;`, `&middot;`, `&#9650;`/`&#9660;`), making the body pure ASCII in source — renders correctly in any client regardless of how it guesses encoding, and is inspectable as plain text without relying on a particular viewer's charset detection.
- **Send to yourself by default.** `EMAIL_TO` in `.env` is optional and falls back to `EMAIL_ADDRESS` — the simplest working setup is one Gmail account emailing its own inbox, which Gmail trusts without any deliverability/spam concerns. `EMAIL_TO` stays available for pointing elsewhere later without changing code.

## Repo restructuring

- **Package named `renpho_tool/`, not `renpho/`.** The obvious name collides with the pip-installed client library `fetcher.py` already imports (`from renpho import RenphoClient`) — a local `renpho/` folder would shadow the real package on `sys.path` and break every fetch. Caught before it caused a real bug, not after.
- **Two folders, not a deep hierarchy.** `renpho_tool/` (the pipeline) and `report_assets/` (the report's HTML/CSS/JS) map to "backend"/"frontend" without actually being a client-server app — this isn't Flask, there's no server, just a Python program that happens to generate a web page. Entry points (`run.py`, `weekly_check.py`) and configs stay at root rather than nesting further; for ~1500 lines across 8 modules, more nesting would be organizing for its own sake.
- **`report_assets/` is a build-time input, not a runtime dependency.** The whole reason `report.html` is one file (see Phase 3's "the report renders client-side" decision) is so it stays portable — double-click to open, no server, nothing else needs to sit next to it. Splitting the HTML/CSS/JS into real source files could have broken that by linking them (`<link href=...>`, `<script src=...>`), which would mean report.html only works if `report_assets/` travels with it. Instead `report.py` reads the three source files and inlines their full contents into the generated `report.html` at write time — same self-contained output as before, just assembled from modular source instead of one giant Python string.
- **Plain string `.replace()` on marker tokens, not `str.format()`, to inject content into the HTML template.** `str.format()` treats every `{` and `}` in the string as a potential placeholder — and the CSS/JS being inlined is full of literal curly braces (every CSS rule, every JS block). Using unique `__RENPHO_*__` tokens with `.replace()` sidesteps that entirely; unlike the old Python-f-string version, none of the CSS/JS source needs its braces doubled anymore, which was its own long-running source of bugs throughout Phases 3-5.
- **Caught a real marker-collision bug during verification, not after.** The token chosen for injecting the report's config JSON was `__RENPHO_CONFIG__` — the same string as part of the JS global's own name (`window.__RENPHO_CONFIG__`). The naive `.replace()` corrupted the assignment target too (`window.{...json...} = {...json...}`, invalid JS — the whole report would have rendered blank). Found by grepping the actual generated `report.html` for leftover/mangled markers rather than assuming the refactor worked because Python raised no errors; fixed by renaming the Python-side token to `__RENPHO_CONFIG_JSON__`, which shares no substring with the JS global's name.
- **The two report-only color constants became CSS custom properties, not duplicated hex.** `COLOR_GOOD`/`COLOR_BAD` are Python constants (also read by `notify.py` for the email) that a few CSS rules need too. Rather than have `report.py` inject their literal values into now-static `styles.css` (reintroducing a Python/CSS coupling this refactor was trying to remove), the CSS defines `--color-good`/`--color-bad` custom properties with the current hex values as their real defaults — a comment notes they must stay in sync with the Python constants by hand, an explicit documented tradeoff rather than a silent one.
- **`window.__RENPHO_CONFIG__` global, not per-value string interpolation, bridges Python config into the now-static `report.js`.** Before this refactor, `GOAL_MODE`, `COLOR_GOOD`, `WEEKS_SHOWN`, etc. were interpolated directly into the JS text by Python. A fully static `report.js` can't have Python values baked into it, so `report.py` now emits one small `<script>window.__RENPHO_CONFIG__ = {...}</script>` block before `report.js` loads, and `report.js` reads everything through `const CONFIG = window.__RENPHO_CONFIG__` at the top — one bridge point instead of seven scattered interpolations.
- **`spike.py` moved into `scripts/exploration/`, alongside `inspect_fields.py`/`inspect_invalid_flag.py`.** All three are one-off investigation scripts that already did their job (verifying the API works, finding the unique key, ruling out `invalidFlag`) — none are part of the active pipeline `run.py`/`weekly_check.py` actually call. Grouping them together was already the established pattern from Phase 1; `spike.py` had just been left at the root since Phase 0.
- **`.env.example` committed with placeholder values, `.env` itself never is.** Makes "clone the repo, add your own credentials, it works for your data" an explicit, discoverable step (`cp .env.example .env`) instead of something only explained in prose in the README's Setup section.
